services:
  media-api:
    image: ghcr.io/toestre/media-tools:latest
    ports:
      - "3001:3000"
    environment:
      - API_KEY=dev-secret
    volumes:
      - ./templates:/templates:ro
      - /tmp/media-api:/tmp
